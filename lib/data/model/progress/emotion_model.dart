class EmotionModel {
  String title;
  String status;
  int progress;
  int total_progress;
  EmotionModel({
    required this.title,
    required this.status,
    required this.progress,
    required this.total_progress,
  });
}
